# Reuse the existing reviewed Linux amd64 / Python 3.12 runtime resolution.
# Installed outline work needs the same native geometry/source readers.
-r ../../deployment/application-runtime-requirements.txt
