# Publish EOLab on the host loopback interface for manual Docker Compose runs.
services:
  app:
    ports:
      - "127.0.0.1:${EOLAB_HOST_PORT:-8000}:8000"
  geoserver:
    ports:
      - "127.0.0.1:${EOLAB_GEOSERVER_HOST_PORT:-8081}:8080"
